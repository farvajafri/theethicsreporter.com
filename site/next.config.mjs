/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  // Citadel articles: Kevin Nutter investigation published 2026-05-14
};

export default nextConfig;
